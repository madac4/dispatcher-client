export enum CookieKeys {
  ACCESS_TOKEN = 'accessToken',
  REFRESH_TOKEN = 'refreshToken',
}

export enum StorageVariables {
  ASIDE_OPEN = 'isAsideOpen',
}

export enum DialogActions {
  Edit = 'edit',
  Create = 'create',
}
